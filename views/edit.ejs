<!DOCTYPE html>
<html lang="en">
    <%- include("./partials/head.ejs") %>
<body>
    <%- include("./partials/header.ejs") %>
    <div class="container-new">
    <h1>Edit Post Information</h1>
    <div class="form-new">
    <form action="/posts/<%= index %>?_method=PUT" method="POST">

        <div class="name-new">
            <label for="name">Name</label>
            <input type="text" name="name" id="name" value="<%= post.name %>"/>            
        </div>

        <div class="img-new">
            <label for="img">Image Address</label>
            <input type="text" name="img" id="img" value="<%= post.img %>"/>            
        </div>

        <div class="age-new">
            <label for="age">Age</label>
            <input type="text" name="age" id="age" value="<%= post.age %>"/>            
        </div>

        <div class="des-new">
            <label for="description">Description</label>
            <input type="text" name="description" id="description" value="<%= post.description %>"/>            
        </div>

        <div class="trained-new">
            <input type="checkbox" name="trained" id="trained" <% if(post.trained){ %> checked <% } %> /> 

    
            <label for="trained">Trained?</label>            
        </div>

        <div class="breed-new">
            <label for="breed">Breed</label>
            <input type="text" name="breed" id="breed" value="<%= post.breed %>"/>            
        </div>


        <input type="submit" value="Edit Adoption Post" class="btn1"/>
    </div>
    </form>
</div>
</body>
</html>